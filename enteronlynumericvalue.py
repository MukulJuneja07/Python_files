#while(1):
    #x=input("Enter any number")
    #if(x.isdigit()):
        #x=int(x)
        #print(x)
        #break
    #else:
        #print("Enter input only in integer")


while(1):
    while(1):
        x=input("Enter first number")
        if(x.isdigit()):
            x=int(x)
            break
        else:
            print("Enter input only in integer")
    while(1):
        y=input("Enter second number")
        if(y.isdigit()):
            y=int(y)
            break
        else:
            print("Enter input only in integer")
    print(x,y)